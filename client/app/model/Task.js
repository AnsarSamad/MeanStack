"use strict";
class Task {
    constructor(title, done) {
        this.title = title;
        this.isDone = done;
    }
    setContent(content) {
        this.content = content;
    }
    setPrice(pr) {
        this.price = pr;
    }
}
exports.Task = Task;
//# sourceMappingURL=Task.js.map